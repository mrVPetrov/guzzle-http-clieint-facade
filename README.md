# guzzle-http-clieint-facade

guzzlehttp/client request options are passed as kay => value arrays and IDE can not autocomplete them or give you a hint about possible keys and values
So, I decided to wrap it in IDE friendly facade class thet receives it's options via typed objects with typed fields.   